# MOMent(맘달) 데이터베이스 요구사항 분석서

## 1. 문서 목적

본 문서는 ERD.png에 정의된 MOMent 데이터베이스 구조를 기준으로 각 테이블의 목적, 컬럼 의미, 관계, 사용 화면, 사용 기능을 하나씩 설명한다. 평가자가 ERD를 처음 보더라도 “왜 이 테이블이 필요한지”, “어떤 화면에서 쓰이는지”, “어떤 데이터 흐름을 만드는지” 이해할 수 있도록 작성한다.

## 2. ERD 전체 개요

MOMent의 데이터베이스는 크게 8개 영역으로 나뉜다.

| 영역 | 관련 테이블 | 목적 |
|---|---|---|
| 사용자/관계 | `USERS`, `USER_CARE_PREFERENCES` | 임산부, 보호자, 관리자 계정과 개인 맞춤 설정을 관리한다. |
| 상태 체크/미션 | `PREGNANCY_STATUS_CHECKS`, `GUARDIAN_MISSIONS` | 임산부 상태 입력을 보호자 미션으로 연결한다. |
| 감정 일기 | `DIARY_LOGS`, `DIARY_SYMPTOMS`, `AI_ANALYSIS_RESULTS` | 일기, 증상, 감정 분석 결과를 저장한다. |
| 스몰토크 | `SMALL_TALK_TOPICS`, `SMALL_TALK_ANSWERS` | 임산부와 보호자의 질문/답변을 저장한다. |
| 커뮤니티 | `COMMUNITY_POSTS`, `COMMUNITY_COMMENTS`, `COMMUNITY_POST_LIKES` | 게시글, 댓글, 좋아요를 저장한다. |
| 일정 | `SHARED_CALENDAR_EVENTS` | 임산부와 보호자가 함께 보는 일정을 저장한다. |
| 추천/정보 | `WEEKLY_AI_RECOMMENDATIONS`, `TRUSTED_PREGNANCY_INFO` | 주차별 추천과 신뢰 정보를 저장한다. |
| 가전 제어 | `APPLIANCE_SETTINGS` | 감정/환경 기반 가전 세팅 추천을 저장한다. |

## 3. 테이블별 상세 정의

### 3.1 USERS

`USERS`는 모든 사용자의 기본 계정 정보를 저장하는 중심 테이블이다. 임산부, 보호자, 관리자 모두 이 테이블에 저장된다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `user_id` | BIGINT | PK | 사용자 고유 ID |
| `email` | VARCHAR(100) | Unique | 로그인 ID |
| `password` | VARCHAR(255) |  | 비밀번호 |
| `name` | VARCHAR(50) |  | 사용자 이름 |
| `role` | VARCHAR(20) |  | 사용자 역할. 임산부, 보호자, 관리자 구분 |
| `pregnancy_start_date` | DATE |  | 임신 시작일. 현재 임신 주차 계산에 사용 |
| `baby_nickname` | VARCHAR(50) |  | 태명 |
| `connection_code` | VARCHAR(100) |  | 임산부와 보호자 연동 코드 |
| `parent_user_id` | BIGINT | FK 성격 | 연결된 상대 사용자 ID |
| `created_at` | TIMESTAMP |  | 가입일 |

사용 목적:

| 사용 화면 | 사용 내용 |
|---|---|
| 로그인 | 이메일과 비밀번호 확인 |
| 회원가입 | 신규 사용자 저장 |
| 대시보드 | 임신 시작일로 현재 주차 계산 |
| 보호자 미션 | 연결 코드로 보호자와 임산부 관계 확인 |
| 관리자 | 전체 회원 목록, 역할 구분, 대리 접속 |

관계:

| 연결 테이블 | 관계 | 설명 |
|---|---|---|
| `DIARY_LOGS` | 1:N | 한 사용자는 여러 일기를 작성한다. |
| `COMMUNITY_POSTS` | 1:N | 한 사용자는 여러 게시글을 작성한다. |
| `COMMUNITY_COMMENTS` | 1:N | 한 사용자는 여러 댓글을 작성한다. |
| `COMMUNITY_POST_LIKES` | 1:N | 한 사용자는 여러 게시글에 좋아요를 누른다. |
| `SMALL_TALK_ANSWERS` | 1:N | 한 사용자는 여러 스몰토크 답변을 작성한다. |
| `PREGNANCY_STATUS_CHECKS` | 1:N | 한 임산부는 여러 상태 체크를 작성한다. |
| `GUARDIAN_MISSIONS` | 1:N | 보호자 또는 대상 사용자에게 여러 미션이 연결된다. |
| `USER_CARE_PREFERENCES` | 1:1 | 한 사용자는 하나의 개인 맞춤 설정을 가진다. |

### 3.2 USER_CARE_PREFERENCES

`USER_CARE_PREFERENCES`는 보호자 미션을 만들 때 사용자의 선호를 반영하기 위한 테이블이다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `preference_id` | BIGINT | PK | 맞춤 설정 ID |
| `user_id` | BIGINT | FK | 사용자 ID |
| `preferred_mission_type` | VARCHAR(50) |  | 선호 미션 유형. 예: 정서 지원, 생활 지원, 건강 체크 |
| `notification_enabled` | TINYINT(1) |  | 알림 사용 여부 |
| `mission_time` | VARCHAR(20) |  | 미션 알림 선호 시간 |
| `care_style` | VARCHAR(50) |  | 케어 스타일. 예: 따뜻하게, 간단하게, 적극적으로 |
| `avoid_mission_keywords` | TEXT |  | 피하고 싶은 미션 키워드 |
| `updated_at` | DATETIME |  | 수정일 |

사용 목적:

- 임산부가 부담스럽게 느끼는 미션을 피한다.
- 보호자에게 맞는 케어 방식을 적용한다.
- 상태 체크 결과를 단순 문구가 아니라 개인 맞춤 미션으로 바꾼다.

### 3.3 PREGNANCY_STATUS_CHECKS

`PREGNANCY_STATUS_CHECKS`는 임산부가 “오늘의 상태 체크”에서 입력한 증상과 감정을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `status_check_id` | BIGINT | PK | 상태 체크 ID |
| `user_id` | BIGINT | FK | 임산부 사용자 ID |
| `symptoms` | TEXT |  | 오늘 느끼는 신체 증상 |
| `emotions` | TEXT |  | 오늘 느끼는 감정 |
| `created_at` | DATETIME |  | 체크 시간 |

중요한 설계 의도:

- 이 테이블은 일기 저장용이 아니다.
- 임산부가 상태 체크를 하면 보호자 미션 생성으로 이어진다.
- 보호자에게 상세 감정 원문을 그대로 보여주기보다 미션으로 변환한다.

관계:

| 연결 테이블 | 관계 | 설명 |
|---|---|---|
| `USERS` | N:1 | 한 임산부가 여러 상태 체크를 작성한다. |
| `GUARDIAN_MISSIONS` | 1:N | 상태 체크 하나가 하나 이상의 미션 생성 근거가 될 수 있다. |

### 3.4 GUARDIAN_MISSIONS

`GUARDIAN_MISSIONS`는 보호자에게 전달되는 미션을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `mission_id` | BIGINT | PK | 미션 ID |
| `analysis_id` | BIGINT | FK | 감정 분석 결과 ID |
| `user_id` | BIGINT | FK | 미션 대상 사용자 |
| `mission_content` | TEXT |  | 미션 내용 |
| `execution_status` | VARCHAR(50) |  | 수행 상태 |
| `created_at` | TIMESTAMP |  | 생성일 |
| `status_check_id` | BIGINT | FK | 상태 체크 ID |
| `mission_title` | VARCHAR(200) |  | 미션 제목 |
| `mission_reason` | TEXT |  | 미션 생성 이유 |
| `mission_type` | VARCHAR(50) |  | 미션 유형 |
| `completed_at` | DATETIME |  | 완료 시간 |

사용 예시:

| 상태 체크 내용 | 생성될 수 있는 미션 |
|---|---|
| 피곤함, 불안 | “오늘은 집안일을 대신하고 휴식 시간을 만들어 주세요.” |
| 화남, 예민함 | “해결책을 말하기보다 먼저 공감하는 대화를 해 주세요.” |
| 몸이 무거움 | “무리한 외출보다 짧은 산책이나 스트레칭을 제안해 주세요.” |

### 3.5 DIARY_LOGS

`DIARY_LOGS`는 감정 일기를 저장하는 테이블이다. 임산부의 하루 기록, 감정, 환경 데이터를 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `diary_id` | BIGINT | PK | 일기 ID |
| `user_id` | BIGINT | FK | 작성자 ID |
| `temperature_ambient` | DECIMAL(4,2) |  | 기록 시 온도 |
| `humidity_ambient` | DECIMAL(4,2) |  | 기록 시 습도 |
| `weather_ambient` | VARCHAR(50) |  | 날씨 |
| `selected_emotion` | VARCHAR(50) |  | 사용자가 선택하거나 분석된 감정 |
| `stress_level` | INT |  | 스트레스 정도 |
| `diary_content` | TEXT |  | 일기 내용 |
| `small_talk_topic_id` | BIGINT | FK | 관련 스몰토크 주제 |
| `recorded_at` | TIMESTAMP |  | 기록 시간 |
| `image_path` | VARCHAR(255) |  | 첨부 이미지 경로 |

사용 목적:

- 감정 일기 화면에서 기록을 저장한다.
- 감정 분석 결과와 연결된다.
- 관리자 화면에서 감정 분포와 평균 온습도 분석에 활용된다.
- 가전 제어 추천의 입력 데이터로 활용될 수 있다.

### 3.6 DIARY_SYMPTOMS

`DIARY_SYMPTOMS`는 일기와 연결된 증상을 별도로 저장하기 위한 테이블이다. 하나의 일기에 여러 증상을 연결할 수 있게 만든다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `diary_symptom_id` | BIGINT | PK | 일기 증상 ID |
| `diary_id` | BIGINT | FK | 연결된 일기 ID |
| `symptom_name` | VARCHAR(100) |  | 증상 이름 |

설계 이유:

- 일기 하나에 “허리 통증”, “입덧”, “피로”처럼 여러 증상이 들어갈 수 있다.
- 증상을 문자열 하나로 저장하면 통계가 어렵다.
- 별도 테이블로 분리하면 증상별 빈도 분석이 가능하다.

### 3.7 AI_ANALYSIS_RESULTS

`AI_ANALYSIS_RESULTS`는 감정 일기 분석 결과를 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `analysis_id` | BIGINT | PK | 분석 결과 ID |
| `diary_id` | BIGINT | FK | 분석 대상 일기 |
| `detected_emotion` | VARCHAR(50) |  | 분석된 감정 |
| `analyzed_at` | TIMESTAMP |  | 분석 시간 |

사용 목적:

- 감정 일기 자동 분석 결과를 저장한다.
- 보호자 미션 또는 가전 추천의 참고 데이터가 된다.
- 관리자 화면에서 감정 분포를 집계할 수 있다.

### 3.8 APPLIANCE_SETTINGS

`APPLIANCE_SETTINGS`는 가전 제어 추천 결과를 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `setting_id` | BIGINT | PK | 가전 세팅 ID |
| `analysis_id` | BIGINT | FK | 감정 분석 결과 ID |
| `appliance_name` | VARCHAR(100) |  | 가전 이름 |
| `control_command` | VARCHAR(255) |  | 제어 명령 |
| `execution_status` | VARCHAR(50) |  | 실행 상태 |
| `user_id` | BIGINT | FK | 사용자 ID |

사용 예시:

| 가전 | 추천 명령 |
|---|---|
| 에어컨 | 목표 온도 22.6도 |
| 제습기 | 목표 습도 52.6% |
| 무드등 | 밝기 50% |
| 공기청정기 | 풍량 2단 |

관리자 분석에서는 이 값을 평균 내어 “사용자가 만족감을 느끼는 평균 가전 세팅”으로 보여준다.

### 3.9 SMALL_TALK_TOPICS

`SMALL_TALK_TOPICS`는 스몰토크 질문을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `topic_id` | BIGINT | PK | 질문 ID |
| `question_text` | VARCHAR(255) |  | 질문 내용 |
| `created_at` | TIMESTAMP |  | 생성일 |

사용 목적:

- 홈 스몰토크 탭에서 질문을 보여준다.
- 임산부와 보호자가 같은 질문에 답변하도록 기준을 제공한다.

### 3.10 SMALL_TALK_ANSWERS

`SMALL_TALK_ANSWERS`는 스몰토크 질문에 대한 사용자별 답변을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `answer_id` | BIGINT | PK | 답변 ID |
| `topic_id` | BIGINT | FK | 질문 ID |
| `user_id` | BIGINT | FK | 답변 작성자 |
| `connection_code` | VARCHAR(100) |  | 임산부/보호자 연결 코드 |
| `answer_content` | TEXT |  | 답변 내용 |
| `match_status` | VARCHAR(50) |  | 답변 매칭 상태 |
| `created_at` | TIMESTAMP |  | 작성일 |

중요 규칙:

- 임산부 답변만 있으면 다이어리에는 표시하지 않는다.
- 보호자 답변만 있어도 다이어리에는 표시하지 않는다.
- 같은 질문에 두 사람이 모두 답변해야 다이어리 날짜 상세에서 보여준다.

### 3.11 SHARED_CALENDAR_EVENTS

`SHARED_CALENDAR_EVENTS`는 임산부와 보호자가 함께 보는 일정을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `event_id` | BIGINT | PK | 일정 ID |
| `connection_code` | VARCHAR(100) |  | 연결 코드 |
| `event_type` | VARCHAR(50) |  | 일정 유형. 예: 산부인과, 검사, 태교 여행 |
| `title` | VARCHAR(200) |  | 일정 제목 |
| `content` | TEXT |  | 일정 내용 |
| `event_date` | DATE |  | 일정 날짜 |
| `created_at` | TIMESTAMP |  | 생성일 |

사용 목적:

- 다이어리 전체보기에서 공유 일정을 보여준다.
- 임산부와 보호자가 같은 일정 정보를 확인한다.

### 3.12 COMMUNITY_POSTS

`COMMUNITY_POSTS`는 커뮤니티 게시글을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `post_id` | BIGINT | PK | 게시글 ID |
| `user_id` | BIGINT | FK | 작성자 ID |
| `pregnancy_period` | VARCHAR(50) |  | 임신 시기 |
| `title` | VARCHAR(200) |  | 제목 |
| `content` | TEXT |  | 내용 |
| `created_at` | TIMESTAMP |  | 작성일 |

사용 목적:

- 사용자 경험 공유.
- 관리자 커뮤니티 분석의 원본 데이터.
- 임신 시기별 고민 분석.

### 3.13 COMMUNITY_COMMENTS

`COMMUNITY_COMMENTS`는 게시글 댓글을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `comment_id` | BIGINT | PK | 댓글 ID |
| `post_id` | BIGINT | FK | 게시글 ID |
| `user_id` | BIGINT | FK | 작성자 ID |
| `content` | TEXT |  | 댓글 내용 |
| `created_at` | TIMESTAMP |  | 작성일 |

댓글은 커뮤니티 분석에서 게시글과 함께 텍스트 분석 대상이 된다.

### 3.14 COMMUNITY_POST_LIKES

`COMMUNITY_POST_LIKES`는 게시글 좋아요 이력을 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `like_id` | BIGINT | PK | 좋아요 ID |
| `post_id` | BIGINT | FK | 게시글 ID |
| `user_id` | BIGINT | FK | 좋아요를 누른 사용자 |
| `created_at` | DATETIME |  | 좋아요 누른 시간 |

설계 이유:

- 게시글에 `like_count` 숫자만 저장하면 한 사용자가 여러 번 누르는 것을 막기 어렵다.
- 별도 이력 테이블을 두면 누가 어떤 게시글에 좋아요를 눌렀는지 확인할 수 있다.
- `post_id + user_id` 조합을 유일하게 관리하면 계정당 1회 제한이 가능하다.

### 3.15 WEEKLY_AI_RECOMMENDATIONS

`WEEKLY_AI_RECOMMENDATIONS`는 임신 주차별 추천 정보를 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `recommendation_id` | BIGINT | PK | 추천 ID |
| `pregnancy_week` | INT |  | 임신 주차 |
| `recommendation_type` | VARCHAR(50) |  | 추천 유형. 식품, 활동, 주의사항 등 |
| `title` | VARCHAR(200) |  | 추천 제목 |
| `content` | TEXT |  | 추천 내용 |

사용 목적:

- 현재 임신 주차에 맞는 추천을 보여준다.
- 대시보드 오늘의 팁과 AI 맞춤 추천 화면에 활용한다.

### 3.16 TRUSTED_PREGNANCY_INFO

`TRUSTED_PREGNANCY_INFO`는 공신력 있는 임신 정보를 저장한다.

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `info_id` | BIGINT | PK | 정보 ID |
| `title` | VARCHAR(200) |  | 정보 제목 |
| `content` | TEXT |  | 정보 내용 |
| `source` | VARCHAR(100) |  | 출처 |
| `pregnancy_period` | VARCHAR(50) |  | 임신 시기 |

사용 목적:

- 영양, 운동, 정신건강, 태아발달, 수면 정보를 제공한다.
- 정부 혜택 정보와 함께 공신력 있는 정보 화면을 구성한다.

## 4. 관계 상세 정리

| 기준 테이블 | 대상 테이블 | 관계 | 설명 |
|---|---|---|---|
| `USERS` | `DIARY_LOGS` | 1:N | 한 사용자는 여러 일기를 작성한다. |
| `DIARY_LOGS` | `DIARY_SYMPTOMS` | 1:N | 한 일기에 여러 증상을 저장한다. |
| `DIARY_LOGS` | `AI_ANALYSIS_RESULTS` | 1:1 | 한 일기는 하나의 감정 분석 결과를 가진다. |
| `AI_ANALYSIS_RESULTS` | `APPLIANCE_SETTINGS` | 1:N | 분석 결과를 바탕으로 여러 가전 세팅이 추천될 수 있다. |
| `AI_ANALYSIS_RESULTS` | `GUARDIAN_MISSIONS` | 1:N | 분석 결과가 미션 생성 근거로 사용될 수 있다. |
| `USERS` | `PREGNANCY_STATUS_CHECKS` | 1:N | 임산부는 여러 상태 체크를 작성한다. |
| `PREGNANCY_STATUS_CHECKS` | `GUARDIAN_MISSIONS` | 1:N | 상태 체크 결과로 미션을 생성한다. |
| `USERS` | `USER_CARE_PREFERENCES` | 1:1 | 사용자는 하나의 맞춤 설정을 가진다. |
| `USERS` | `COMMUNITY_POSTS` | 1:N | 사용자는 여러 게시글을 작성한다. |
| `COMMUNITY_POSTS` | `COMMUNITY_COMMENTS` | 1:N | 게시글은 여러 댓글을 가진다. |
| `COMMUNITY_POSTS` | `COMMUNITY_POST_LIKES` | 1:N | 게시글은 여러 좋아요 기록을 가진다. |
| `USERS` | `COMMUNITY_POST_LIKES` | 1:N | 사용자는 여러 게시글에 좋아요를 누른다. |
| `SMALL_TALK_TOPICS` | `SMALL_TALK_ANSWERS` | 1:N | 하나의 질문에 여러 답변이 연결된다. |
| `USERS` | `SMALL_TALK_ANSWERS` | 1:N | 사용자는 여러 스몰토크 답변을 작성한다. |
| `USERS.connection_code` | `SHARED_CALENDAR_EVENTS.connection_code` | 1:N | 같은 연결 코드 사용자가 일정을 공유한다. |

## 5. 데이터 흐름

### 5.1 상태 체크에서 보호자 미션까지

1. 임산부가 오늘의 상태 체크 화면에서 증상과 감정을 입력한다.
2. 서버가 `PREGNANCY_STATUS_CHECKS`에 저장한다.
3. 서버가 `USER_CARE_PREFERENCES`를 확인한다.
4. 임산부 상태와 맞춤 설정을 바탕으로 `GUARDIAN_MISSIONS`를 생성한다.
5. 보호자는 미션 화면에서 수행할 일을 확인한다.

### 5.2 감정 일기에서 가전 추천까지

1. 임산부가 감정 일기를 작성한다.
2. `DIARY_LOGS`에 일기와 온도, 습도, 날씨가 저장된다.
3. 감정 분석 결과가 `AI_ANALYSIS_RESULTS`에 저장된다.
4. 분석 결과와 환경 데이터를 바탕으로 `APPLIANCE_SETTINGS`가 생성된다.
5. 가전 제어 화면과 관리자 평균 가전 세팅 분석에 활용된다.

### 5.3 커뮤니티 분석 흐름

1. 사용자가 `COMMUNITY_POSTS`에 게시글을 작성한다.
2. 사용자가 `COMMUNITY_COMMENTS`에 댓글을 작성한다.
3. 사용자가 `COMMUNITY_POST_LIKES`에 좋아요를 남긴다.
4. 관리자가 분석 버튼을 누르면 게시글과 댓글 텍스트를 가져온다.
5. 불용어를 제거하고 키워드 빈도를 계산한다.
6. 관리자 화면에 워드클라우드와 감정 힌트를 보여준다.

## 6. 무결성 규칙

| 규칙 | 설명 |
|---|---|
| 이메일 중복 금지 | `USERS.email`은 중복되면 안 된다. |
| 좋아요 중복 금지 | 같은 `post_id`, `user_id` 조합은 한 번만 저장한다. |
| 일기와 분석 결과 연결 | 분석 결과는 반드시 특정 일기와 연결되어야 한다. |
| 댓글과 게시글 연결 | 댓글은 반드시 특정 게시글에 연결되어야 한다. |
| 상태 체크와 미션 연결 | 상태 체크 기반 미션은 어떤 상태 체크에서 생성되었는지 추적 가능해야 한다. |
| 공유 일정 연결 | 공유 일정은 연결 코드 기준으로 임산부와 보호자에게 함께 보여야 한다. |

## 7. ERD 기준 구현 보완 사항

| 항목 | 설명 |
|---|---|
| `DIARY_SYMPTOMS` | ERD에는 존재하므로 증상별 통계를 위해 유지하는 것이 맞다. 현재 코드에서 완전히 사용하지 않는다면 향후 일기 증상 다중 선택 기능과 연결해야 한다. |
| `created_at` | ERD 기준 `USERS`에도 가입일이 있으므로 회원 관리와 오늘 가입자 집계를 위해 유지하는 것이 좋다. |
| `COMMUNITY_POST_LIKES` | 좋아요 기능을 위해 ERD에 추가된 핵심 테이블이다. 게시글 테이블에 카운트 컬럼을 추가하는 방식보다 이력 관리에 적합하다. |
| `status_check_id` | 보호자 미션이 일기 분석뿐 아니라 오늘의 상태 체크에서 생성된다는 점을 명확히 하기 위해 필요하다. |

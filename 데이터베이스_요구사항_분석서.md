# MOMent(맘달) 데이터베이스 요구사항 분석서

## 1. 문서 목적

본 문서는 MOMent 프로젝트의 실제 데이터베이스 구조를 기준으로 엔티티, 속성, 관계, 제약조건을 정리한 산출물이다. `정의서 정리.xlsx`의 DB 요구사항 분석 기준인 화면 기반 엔티티 도출, 관계 정의, PK/FK 식별, 정규화, 물리 테이블 설계 항목을 반영했다.

## 2. 데이터 도출 기준

| 화면/기능 | 필요한 데이터 | 도출 엔티티 |
|---|---|---|
| 로그인/회원가입 | 이메일, 비밀번호, 이름, 역할, 임신 시작일, 연결 코드 | `USERS` |
| 감정 일기 | 감정, 내용, 날씨, 온도, 습도, 이미지 | `DIARY_LOGS`, `AI_ANALYSIS_RESULTS` |
| 오늘의 상태 체크 | 증상, 감정, 작성자, 작성일 | `PREGNANCY_STATUS_CHECKS` |
| 보호자 미션 | 미션 제목, 내용, 이유, 유형, 수행 상태 | `GUARDIAN_MISSIONS`, `USER_CARE_PREFERENCES` |
| 스몰토크 | 질문, 사용자별 답변, 연결 코드 | `SMALL_TALK_TOPICS`, `SMALL_TALK_ANSWERS` |
| 공유 일정 | 일정 유형, 제목, 내용, 날짜, 연결 코드 | `SHARED_CALENDAR_EVENTS` |
| 커뮤니티 | 게시글, 댓글, 좋아요 | `COMMUNITY_POSTS`, `COMMUNITY_COMMENTS`, `COMMUNITY_POST_LIKES` |
| 주차별 추천 | 임신 주차, 추천 유형, 제목, 내용 | `WEEKLY_AI_RECOMMENDATIONS` |
| 신뢰 정보 | 제목, 내용, 출처, 임신 시기 | `TRUSTED_PREGNANCY_INFO` |
| 가전 제어 | 가전 이름, 제어 명령, 실행 상태 | `APPLIANCE_SETTINGS` |
| 관리자 분석 | 회원 수, 오늘 접속자, 커뮤니티 키워드, 평균 세팅 | 기존 테이블 집계 |

## 3. 엔티티 정의

| 엔티티 | 테이블명 | 설명 |
|---|---|---|
| 사용자 | `USERS` | 임산부, 보호자, 관리자 계정을 저장한다. |
| 감정 일기 | `DIARY_LOGS` | 사용자의 일기와 감정, 날씨/온습도 정보를 저장한다. |
| AI 분석 결과 | `AI_ANALYSIS_RESULTS` | 일기 자동 감정 분석 결과를 저장한다. |
| 오늘의 상태 체크 | `PREGNANCY_STATUS_CHECKS` | 임산부가 입력한 오늘 증상과 감정을 저장한다. |
| 보호자 미션 | `GUARDIAN_MISSIONS` | 상태 체크 기반으로 보호자에게 전달되는 미션을 저장한다. |
| 개인 맞춤 설정 | `USER_CARE_PREFERENCES` | 보호자 미션 생성 시 반영할 케어 선호도를 저장한다. |
| 스몰토크 주제 | `SMALL_TALK_TOPICS` | 하루 질문 또는 관계 질문을 저장한다. |
| 스몰토크 답변 | `SMALL_TALK_ANSWERS` | 질문에 대한 임산부/보호자 답변을 저장한다. |
| 공유 일정 | `SHARED_CALENDAR_EVENTS` | 연결된 사용자들이 함께 보는 일정을 저장한다. |
| 커뮤니티 게시글 | `COMMUNITY_POSTS` | 커뮤니티 게시글을 저장한다. |
| 커뮤니티 댓글 | `COMMUNITY_COMMENTS` | 게시글 댓글을 저장한다. |
| 커뮤니티 좋아요 | `COMMUNITY_POST_LIKES` | 게시글 좋아요 이력을 저장한다. |
| 가전 세팅 | `APPLIANCE_SETTINGS` | 추천 또는 시연용 가전 제어 값을 저장한다. |
| 주차별 추천 | `WEEKLY_AI_RECOMMENDATIONS` | 임신 주차별 추천 콘텐츠를 저장한다. |
| 신뢰 정보 | `TRUSTED_PREGNANCY_INFO` | 공신력 있는 임신 정보를 저장한다. |

## 4. 테이블 상세

### 4.1 USERS

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `user_id` | Integer | PK | 사용자 식별자 |
| `email` | String(100) | Unique | 로그인 ID |
| `password` | String(255) |  | 비밀번호 |
| `name` | String(50) |  | 이름 |
| `role` | String(20) |  | `PREGNANT`, `GUARDIAN`, `ADMIN` |
| `baby_nickname` | String(50) |  | 태명 |
| `pregnancy_start_date` | Date |  | 임신 시작일 |
| `connection_code` | String(20) |  | 보호자 연동 코드 |
| `parent_user_id` | Integer |  | 연결 대상 사용자 ID |

### 4.2 DIARY_LOGS

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `diary_id` | Integer | PK | 일기 ID |
| `user_id` | Integer | FK | 작성자 |
| `temperature_ambient` | DECIMAL(4,2) |  | 체감/환경 온도 |
| `humidity_ambient` | DECIMAL(4,2) |  | 체감/환경 습도 |
| `weather_ambient` | String(50) |  | 날씨 |
| `selected_emotion` | String(50) |  | 선택 또는 분석된 감정 |
| `stress_level` | Integer |  | 스트레스 정도 |
| `diary_content` | Text |  | 일기 내용 |
| `small_talk_topic_id` | Integer |  | 연결된 스몰토크 주제 |
| `recorded_at` | DateTime |  | 작성 시각 |
| `image_path` | String(255) |  | 이미지 경로 |

### 4.3 PREGNANCY_STATUS_CHECKS

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `status_check_id` | BigInteger | PK | 상태 체크 ID |
| `user_id` | BigInteger | FK | 임산부 사용자 ID |
| `symptoms` | Text |  | 증상 |
| `emotions` | Text |  | 기분/감정 |
| `created_at` | DateTime |  | 작성 시각 |

### 4.4 GUARDIAN_MISSIONS

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `mission_id` | BigInteger | PK | 미션 ID |
| `analysis_id` | BigInteger | FK Nullable | 감정 분석 결과 |
| `status_check_id` | BigInteger | FK Nullable | 상태 체크 ID |
| `user_id` | BigInteger | FK | 보호자 또는 대상 사용자 |
| `mission_title` | String(200) |  | 미션 제목 |
| `mission_content` | Text |  | 미션 내용 |
| `mission_reason` | Text |  | 미션 생성 이유 |
| `mission_type` | String(50) |  | 미션 유형 |
| `execution_status` | String(50) |  | `PENDING`, `DONE` 등 |
| `created_at` | DateTime |  | 생성일 |
| `completed_at` | DateTime |  | 완료일 |

### 4.5 COMMUNITY_POSTS / COMMENTS / LIKES

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `COMMUNITY_POSTS` | `post_id`, `user_id`, `pregnancy_period`, `title`, `content`, `created_at` | 게시글 |
| `COMMUNITY_COMMENTS` | `comment_id`, `post_id`, `user_id`, `content`, `created_at` | 댓글 |
| `COMMUNITY_POST_LIKES` | `like_id`, `post_id`, `user_id`, `created_at` | 좋아요 이력 |

좋아요 수는 게시글 테이블에 숫자 컬럼으로 저장하지 않는다. `COMMUNITY_POST_LIKES`를 집계하여 계산한다. 한 사용자가 같은 게시글에 여러 번 좋아요를 누르지 못하도록 `post_id`, `user_id` 조합에 유일 제약을 둔다.

### 4.6 SMALL_TALK_TOPICS / ANSWERS

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `SMALL_TALK_TOPICS` | `topic_id`, `question_text`, `created_at` | 질문 |
| `SMALL_TALK_ANSWERS` | `answer_id`, `topic_id`, `user_id`, `connection_code`, `answer_content`, `match_status`, `created_at` | 사용자별 답변 |

다이어리 탭에서는 같은 연결 코드와 같은 질문에 대해 임산부와 보호자 답변이 모두 존재할 때만 표시한다. 홈의 스몰토크 기능은 그대로 유지한다.

### 4.7 APPLIANCE_SETTINGS

| 컬럼 | 타입 | 키 | 설명 |
|---|---|---|---|
| `setting_id` | BigInteger | PK | 세팅 ID |
| `analysis_id` | BigInteger |  | 분석 결과 ID |
| `user_id` | BigInteger |  | 사용자 ID |
| `appliance_name` | String(50) |  | 가전 이름 |
| `control_command` | Text |  | JSON 형태 제어 명령 |
| `execution_status` | String(20) |  | 실행 상태 |

`control_command`에는 온도, 습도, 밝기, 풍량, 공기청정 세기 등 Arduino 시연과 관리자 평균 세팅 분석에 필요한 값이 JSON 문자열로 저장된다.

## 5. 관계 정의

| 관계 | 카디널리티 | 설명 |
|---|---|---|
| `USERS` - `DIARY_LOGS` | 1:N | 한 사용자는 여러 감정 일기를 작성한다. |
| `DIARY_LOGS` - `AI_ANALYSIS_RESULTS` | 1:0..1 | 한 일기는 감정 분석 결과를 하나 가질 수 있다. |
| `USERS` - `PREGNANCY_STATUS_CHECKS` | 1:N | 한 임산부는 여러 상태 체크를 작성한다. |
| `PREGNANCY_STATUS_CHECKS` - `GUARDIAN_MISSIONS` | 1:N | 상태 체크를 기반으로 미션이 생성된다. |
| `USERS` - `USER_CARE_PREFERENCES` | 1:0..1 | 사용자는 맞춤 설정을 하나 가질 수 있다. |
| `USERS` - `COMMUNITY_POSTS` | 1:N | 사용자는 여러 게시글을 작성한다. |
| `COMMUNITY_POSTS` - `COMMUNITY_COMMENTS` | 1:N | 게시글에는 여러 댓글이 달린다. |
| `COMMUNITY_POSTS` - `COMMUNITY_POST_LIKES` | 1:N | 게시글에는 여러 좋아요가 기록된다. |
| `USERS` - `COMMUNITY_POST_LIKES` | 1:N | 사용자는 여러 게시글에 좋아요를 누를 수 있다. |
| `SMALL_TALK_TOPICS` - `SMALL_TALK_ANSWERS` | 1:N | 하나의 질문에 여러 사용자 답변이 연결된다. |
| `USERS.connection_code` - `SHARED_CALENDAR_EVENTS.connection_code` | 1:N | 같은 연결 코드를 가진 사용자가 공유 일정을 본다. |

## 6. 제약조건과 무결성

| 항목 | 내용 |
|---|---|
| 사용자 중복 방지 | `USERS.email`은 유일해야 한다. |
| 좋아요 중복 방지 | `COMMUNITY_POST_LIKES(post_id, user_id)`는 유일해야 한다. |
| 개인 설정 중복 방지 | `USER_CARE_PREFERENCES.user_id`는 유일해야 한다. |
| 분석 결과 중복 방지 | `AI_ANALYSIS_RESULTS.diary_id`는 유일해야 한다. |
| 게시글 삭제 | 게시글 삭제 시 해당 좋아요와 댓글을 함께 삭제한다. |
| 회원 삭제 | 관리자 삭제 시 일기, 분석, 커뮤니티, 스몰토크, 미션, 상태 체크, 개인 설정 데이터를 함께 정리한다. |

## 7. 운영/배포 고려사항

| 항목 | 내용 |
|---|---|
| DB 연결 | SQLAlchemy 엔진으로 MySQL 연결 |
| 서버 배포 | Render FastAPI |
| 프론트 배포 | Vercel React/Vite |
| 메모리 제한 | Render Free 512MB 기준, 서버 시작 시 AI 모델 기본 로딩 금지 |
| 관리자 분석 | Kiwi 같은 무거운 분석기 대신 경량 정규식 기반 토큰 분석 사용 |
| 환경변수 | DB URL, API 키, 허용 Origin은 환경변수로 관리 |

## 8. ERD 변경 반영 사항

| 변경 | 이유 |
|---|---|
| `COMMUNITY_POST_LIKES` 추가 | 계정당 1회 좋아요를 보장하기 위해 별도 이력 테이블이 필요하다. |
| `PREGNANCY_STATUS_CHECKS`와 `GUARDIAN_MISSIONS` 연결 | 일기 저장이 아니라 상태 체크 후 보호자 미션이 생성되는 구조를 반영한다. |
| `USER_CARE_PREFERENCES` 유지 | 보호자 미션을 개인 맞춤형으로 생성하기 위해 필요하다. |
| 게시글에 `like_count` 컬럼 미추가 | 좋아요 수는 이력 테이블 집계로 계산하는 것이 무결성에 유리하다. |

## 9. 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-06-02 | 실제 프로젝트 테이블과 관리자/커뮤니티/상태 체크 기능 기준으로 DB 문서 재작성 |
| 2026-06-02 | 커뮤니티 좋아요 이력 테이블과 유일 제약 반영 |
| 2026-06-02 | Render 메모리 제한과 관리자 분석 경량화 반영 |

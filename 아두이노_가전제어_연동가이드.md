# MOMent 가전제어 탭 - Arduino 최종 연동 가이드

> 최종 배선 기준은 이 문서와 `arduino/final/final.ino`이다.

## 1. 시연 구조

```text
React 가전제어 탭 (localhost)
        ↓ REST API
FastAPI 백엔드 (localhost:8000)
        ↓ pyserial / USB
Arduino UNO
        ↓
RGB LED / 가습기 모듈 / DC 모터 4개 / ULN2003 / LCD / 제습기 상태 LED
```

배포된 Render 서버는 발표장 노트북의 USB 포트에 접근할 수 없다. Arduino 시연은 프론트와 백엔드를 로컬호스트로 실행한 상태에서 진행한다.

## 2. 부품별 역할

| 앱 가전 | 실제 부품 | 구현 의미 |
|---|---|---|
| 무드등 | RGB LED 모듈 | 색상과 밝기 변경 |
| 가습기 | 가습기 모듈 | DIN HIGH/LOW로 분무 ON/OFF |
| 제습기 | 빨강·초록 LED | OFF는 빨강, ON은 초록으로 표시 |
| 에어컨 | 5V DC 모터 + ULN2003 | PWM으로 풍량 1·2·3단 표현 |
| 공기청정기 | 5V DC 모터 + ULN2003 | PWM으로 풍량 1·2·3단 표현 |
| 세탁기 | 5V DC 모터 + ULN2003 | ON/OFF 회전 표현 |
| 건조기 | 5V DC 모터 + ULN2003 | ON/OFF 회전 표현 |
| 전체 상태 | I2C 1602 LCD | 현재 모드, 목표 온도, 목표 습도 표시 |
| 예비 시연 | 버튼 | 앱 연결 실패 시 Arduino 단독 시연 모드 전환 |

## 3. 핀맵 표

실제 조립할 때는 아래 표를 기준으로 연결한다. 신호선은 Arduino 핀에 연결하고, `5V`와 `GND`는 전원 분배 라인에 연결한다.

| 구분 | 부품/모듈 | 모듈 쪽 핀 | Arduino/전원 연결 | 선 색상 권장 | 구현 의미 |
|---|---|---|---|---|---|
| 입력 | 시연 모드 버튼 | 한쪽 다리 | D2 | 회색 | 앱 연결 실패 시 Arduino 단독 모드 전환 |
| 입력 | 시연 모드 버튼 | 반대쪽 다리 | GND | 검정 | `INPUT_PULLUP` 방식으로 사용 |
| 표시 | RGB LED 모듈 | R | D3 | 주황 | 감정 기반 무드등 빨강 채널 |
| 표시 | RGB LED 모듈 | G | D5 | 초록 | 감정 기반 무드등 초록 채널 |
| 표시 | RGB LED 모듈 | B | D6 | 파랑 | 감정 기반 무드등 파랑 채널 |
| 표시 | RGB LED 모듈 | GND | GND | 검정 | RGB 모듈 공통 접지 |
| 가습 | 가습기 모듈 | DIN | D4 | 주황 | 가습기 ON/OFF 제어 신호 |
| 가습 | 가습기 모듈 | VCC | 외부 5V | 빨강 | Arduino 5V 단독 사용 비권장 |
| 가습 | 가습기 모듈 | GND | 공통 GND | 검정 | 외부 전원 GND와 Arduino GND 공통 연결 |
| 표시 | 제습기 OFF 빨간 LED | LED + | D7, 220Ω 저항 직렬 | 초록 | 제습기 OFF일 때 켜짐 |
| 표시 | 제습기 OFF 빨간 LED | LED - | GND | 검정 | LED 접지 |
| 표시 | 제습기 ON 초록 LED | LED + | D12, 220Ω 저항 직렬 | 주황 | 제습기 ON일 때 켜짐 |
| 표시 | 제습기 ON 초록 LED | LED - | GND | 회색 | LED 접지 |
| 구동 | 건조기 ULN2003 | IN4 | D8 | 주황 | 건조기 모터 ON/OFF |
| 구동 | 에어컨 ULN2003 | IN1 | D9 | 파랑 | 에어컨 모터 1·2·3단 PWM |
| 구동 | 공기청정기 ULN2003 | IN2 | D10 | 핑크 | 공기청정기 모터 1·2·3단 PWM |
| 구동 | 세탁기 ULN2003 | IN3 | D11 | 노랑 | 세탁기 모터 ON/OFF |
| 표시 | 서버 연결 상태 LED | LED + | D13, 220Ω 저항 직렬 | 초록 | USB/서버 연결 상태 표시 |
| 표시 | 서버 연결 상태 LED | LED - | GND | 검정 | LED 접지 |
| 전원 | ULN2003 | COM | 외부 5V(+) | 빨강 | 모터 보호 다이오드 공통 전원 |
| 전원 | ULN2003 | GND | 외부 5V(-), Arduino GND | 검정 | 모든 장치 공통 접지 |
| 표시 | I2C 1602 LCD | SDA | A4 | 초록 | LCD 데이터 라인 |
| 표시 | I2C 1602 LCD | SCL | A5 | 파랑 | LCD 클럭 라인 |
| 표시 | I2C 1602 LCD | VCC | 5V | 빨강 | LCD 전원 |
| 표시 | I2C 1602 LCD | GND | GND | 검정 | LCD 접지 |

### Arduino 핀 위치 기준 요약

| Arduino 위치 | 사용하는 핀 | 연결 대상 |
|---|---|---|
| 디지털 상단 오른쪽 | D2 | 시연 모드 버튼 |
| 디지털 상단 오른쪽 | D3 | RGB LED R |
| 디지털 상단 오른쪽 | D4 | 가습기 DIN |
| 디지털 상단 오른쪽 | D5 | RGB LED G |
| 디지털 상단 오른쪽 | D6 | RGB LED B |
| 디지털 상단 중앙 | D7 | 제습기 OFF 빨간 LED |
| 디지털 상단 왼쪽 | D8 | 건조기 ULN2003 IN4 |
| 디지털 상단 왼쪽 | D9 | 에어컨 ULN2003 IN1 |
| 디지털 상단 왼쪽 | D10 | 공기청정기 ULN2003 IN2 |
| 디지털 상단 왼쪽 | D11 | 세탁기 ULN2003 IN3 |
| 디지털 상단 왼쪽 | D12 | 제습기 ON 초록 LED |
| 디지털 상단 왼쪽 | D13 | 서버 연결 상태 LED |
| 아날로그 하단 오른쪽 | A4 | I2C LCD SDA |
| 아날로그 하단 오른쪽 | A5 | I2C LCD SCL |
| 전원 하단 왼쪽 | 5V | LCD, 필요 시 저전류 모듈 전원 |
| 전원 하단 왼쪽 | GND | 모든 부품 공통 접지 |

D9와 D10은 PWM 출력으로 에어컨과 공기청정기의 1·2·3단 속도를 표현한다. D8과 D11은 건조기와 세탁기의 ON/OFF 출력으로 사용한다.

## 4. 전원 연결

| 부품 | 연결 |
|---|---|
| Arduino UNO | 노트북 USB 연결 |
| 가습기 모듈 | VCC → 5V, GND → GND, DIN → D4 |
| ULN2003 | IN1 → D9, IN2 → D10, IN3 → D11, IN4 → D8 |
| 에어컨 모터 | 한 선 → 외부 5V(+), 다른 선 → ULN2003 OUT1 |
| 공기청정기 모터 | 한 선 → 외부 5V(+), 다른 선 → ULN2003 OUT2 |
| 세탁기 모터 | 한 선 → 외부 5V(+), 다른 선 → ULN2003 OUT3 |
| 건조기 모터 | 한 선 → 외부 5V(+), 다른 선 → ULN2003 OUT4 |
| I2C LCD | VCC → 5V, GND → GND, SDA → A4, SCL → A5 |
| RGB LED 모듈 | VCC/GND와 D3/D5/D6 연결 |
| 일반 LED | 220Ω 저항을 직렬로 연결 |

DC 모터 4개와 가습기 모듈은 Arduino 5V 핀으로 구동하지 않는다. 정전압 외부 5V 전원을 사용하고 외부 전원 GND, ULN2003 GND, Arduino GND를 공통으로 연결한다.

## 5. 동작 방식

| 앱 설정 | Arduino 동작 |
|---|---|
| 에어컨 ON, 풍량 1·2·3단 | 에어컨 DC 모터 PWM 속도 변경 |
| 공기청정기 ON, 풍량 1·2·3단 | 공기청정기 DC 모터 PWM 속도 변경 |
| 세탁기 ON/OFF | 세탁기 DC 모터 회전/정지 |
| 건조기 ON/OFF | 건조기 DC 모터 회전/정지 |
| 가습기 ON | 가습기 모듈 분무 ON, 제습기 자동 OFF |
| 제습기 ON | 가습기 자동 OFF, 빨간 LED OFF, 초록 LED ON |
| 제습기 OFF | 빨간 LED ON, 초록 LED OFF |
| 무드등 ON | RGB LED 색상/밝기 변경 |

## 6. 실행 순서

1. Arduino UNO를 USB-A to USB-B 케이블로 노트북에 연결한다.
2. `arduino/final/final.ino`를 Arduino IDE로 연다.
3. 보드와 COM 포트를 선택한 뒤 업로드한다.
4. Arduino IDE의 시리얼 모니터를 닫는다.
5. 백엔드를 로컬에서 실행한다.

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --host 127.0.0.1 --port 8000
```

6. 프론트엔드를 로컬에서 실행한다.

```bash
cd Project
npm run dev
```

7. 앱의 가전제어 탭에서 `USB 연결` 버튼을 누른다.
8. `현재 설정 전송` 버튼을 눌러 Arduino에 가전 설정을 보낸다.

## 7. Serial 명령 형식

백엔드는 Arduino에 한 줄 문자열을 보낸다.

```text
SYNC;ML=1,50,0;AC=1,24,2;HU=0,55,2;DH=0,50,2;AP=1,2,0;WM=1;DR=1
```

| 코드 | 의미 |
|---|---|
| `ML` | 무드등: 전원, 밝기, 색상 코드 |
| `AC` | 에어컨: 전원, 목표 온도, 풍량 |
| `HU` | 가습기: 전원, 목표 습도, 세기 |
| `DH` | 제습기: 전원, 목표 습도, 세기 |
| `AP` | 공기청정기: 전원, 풍량, 모드 코드 |
| `WM` | 세탁기: 전원 0 또는 1 |
| `DR` | 건조기: 전원 0 또는 1 |

## 8. 문제 해결

| 증상 | 확인 사항 |
|---|---|E
| USB 연결 실패 | Arduino IDE의 시리얼 모니터를 닫고 다시 연결한다. |
| COM 포트를 못 찾음 | Windows 장치 관리자에서 Arduino COM 번호를 확인한다. |
| LCD가 안 보임 | I2C 주소가 `0x27`인지 확인하고 대비 조절 나사를 돌린다. |
| 모터가 켜지지 않음 | D9/IN1, D10/IN2, D11/IN3, D8/IN4 연결과 공통 GND를 확인한다. |
| 모터 4개 동시 작동 시 느려짐 | 외부 5V 전원의 전류 용량과 ULN2003 발열을 확인한다. |
| 가습기 분무가 약함 | 필터를 충분히 적시고 5V 전원이 안정적인지 확인한다. |
| 앱은 저장되지만 Arduino가 안 움직임 | 프론트와 백엔드가 모두 localhost로 실행 중인지 확인한다. |

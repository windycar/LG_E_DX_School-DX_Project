# MOMent 가전제어 탭 - Arduino USB 연동 가이드

## 1. 시연 구조

```text
React 가전제어 탭 (localhost)
        ↓ REST API
FastAPI 백엔드 (localhost:8000)
        ↓ pyserial / USB
Arduino UNO
        ↓
RGB LED / 가습기 모듈 / 에어컨 스텝모터 / 공기청정기 스텝모터 / LCD / 상태 LED
```

배포된 Render 서버는 발표장 노트북의 USB 포트에 접근할 수 없다. Arduino 시연은 프론트와 백엔드를 로컬호스트로 실행한 상태에서 진행한다.

## 2. 부품별 역할

| 앱 가전 | 실제 부품 | 구현 의미 |
|---|---|---|
| 무드등 | RGB LED 모듈 | 색상과 밝기 변경 |
| 가습기 | 가습기 모듈 | DIN HIGH/LOW로 분무 ON/OFF |
| 제습기 | 빨강 LED | 제습 상태 ON/OFF 표시 |
| 에어컨 | 스텝모터 1 + ULN2003 | 풍량 단계에 따른 풍향 날개 각도 변경 |
| 공기청정기 | 스텝모터 2 + ULN2003 | 공기 순환 팬/회전 모드 표현 |
| 전체 상태 | I2C 1602 LCD | 현재 모드, 목표 온도, 목표 습도 표시 |
| 예비 시연 | 버튼 | 앱 연결 실패 시 Arduino 단독 시연 모드 전환 |

## 3. 핀 배치

| 부품 | Arduino UNO 핀 |
|---|---|
| 시연 모드 버튼 | D2 |
| RGB LED R | D3 |
| 가습기 모듈 DIN | D4 |
| RGB LED G | D5 |
| RGB LED B | D6 |
| 제습기 상태 LED | D7 |
| 에어컨 스텝모터 ULN2003 IN1 | D8 |
| 에어컨 스텝모터 ULN2003 IN2 | D9 |
| 에어컨 스텝모터 ULN2003 IN3 | D10 |
| 에어컨 스텝모터 ULN2003 IN4 | D11 |
| 공기청정기 상태 LED | D12 |
| 연결 상태 LED | D13 |
| 공기청정기 스텝모터 ULN2003 IN1 | A0 |
| 공기청정기 스텝모터 ULN2003 IN2 | A1 |
| 공기청정기 스텝모터 ULN2003 IN3 | A2 |
| 공기청정기 스텝모터 ULN2003 IN4 | A3 |
| I2C LCD SDA | A4 |
| I2C LCD SCL | A5 |

A0~A3는 아날로그 입력 핀이지만 Arduino UNO에서는 디지털 출력처럼 사용할 수 있다. LCD가 A4/A5를 사용하므로 두 번째 스텝모터는 A0~A3에 배치한다.

## 4. 전원 연결

| 부품 | 연결 |
|---|---|
| Arduino UNO | 노트북 USB 연결 |
| 가습기 모듈 | VCC → 5V, GND → GND, DIN → D4 |
| 에어컨 ULN2003 | VCC → 5V, GND → GND, IN1~IN4 → D8~D11 |
| 공기청정기 ULN2003 | VCC → 5V, GND → GND, IN1~IN4 → A0~A3 |
| I2C LCD | VCC → 5V, GND → GND, SDA → A4, SCL → A5 |
| RGB LED 모듈 | VCC/GND와 D3/D5/D6 연결 |
| 일반 LED | 220Ω 저항을 직렬로 연결 |

스텝모터 2개와 가습기 모듈을 같이 쓰면 USB 전원만으로 부족할 수 있다. 불안정하면 외부 5V 전원을 모터와 가습기에 따로 공급하고, 외부 전원 GND와 Arduino GND를 공통으로 연결한다.

## 5. 동작 방식

| 앱 설정 | Arduino 동작 |
|---|---|
| 에어컨 ON, 풍량 1단 | 에어컨 스텝모터가 작은 각도로 이동 |
| 에어컨 ON, 풍량 2단 | 에어컨 스텝모터가 중간 각도로 이동 |
| 에어컨 ON, 풍량 3단 | 에어컨 스텝모터가 큰 각도로 이동 |
| 공기청정기 ON, 풍량 1단 | 공기청정기 스텝모터가 느리게 회전 |
| 공기청정기 ON, 풍량 2단 | 공기청정기 스텝모터가 중간 속도로 회전 |
| 공기청정기 ON, 강력 모드 | 공기청정기 스텝모터가 더 큰 위치로 이동 후 회전 |
| 가습기 ON | 가습기 모듈 분무 ON |
| 제습기 ON | 제습기 상태 LED ON |
| 무드등 ON | RGB LED 색상/밝기 변경 |

## 6. 실행 순서

1. Arduino UNO를 USB-A to USB-B 케이블로 노트북에 연결한다.
2. `arduino/MOMent_Appliance_Demo/MOMent_Appliance_Demo.ino`를 Arduino IDE로 연다.
3. 보드와 COM 포트를 선택한 뒤 업로드한다.
4. Arduino IDE의 시리얼 모니터를 닫는다.
5. 백엔드를 로컬에서 실행한다.

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --host 127.0.0.1 --port 8000
```

6. 프론트엔드를 로컬에서 실행한다.

```bash
cd Project
npm run dev
```

7. 앱의 가전제어 탭에서 `USB 연결` 버튼을 누른다.
8. `현재 설정 전송` 버튼을 눌러 Arduino에 가전 설정을 보낸다.

## 7. Serial 명령 형식

백엔드는 Arduino에 한 줄 문자열을 보낸다.

```text
SYNC;ML=1,50,0;AC=1,24,2;HU=0,55,2;DH=0,50,2;AP=1,2,0
```

| 코드 | 의미 |
|---|---|
| `ML` | 무드등: 전원, 밝기, 색상 코드 |
| `AC` | 에어컨: 전원, 목표 온도, 풍량 |
| `HU` | 가습기: 전원, 목표 습도, 세기 |
| `DH` | 제습기: 전원, 목표 습도, 세기 |
| `AP` | 공기청정기: 전원, 풍량, 모드 코드 |

## 8. 문제 해결

| 증상 | 확인 사항 |
|---|---|
| USB 연결 실패 | Arduino IDE의 시리얼 모니터를 닫고 다시 연결한다. |
| COM 포트를 못 찾음 | Windows 장치 관리자에서 Arduino COM 번호를 확인한다. |
| LCD가 안 보임 | I2C 주소가 `0x27`인지 확인하고 대비 조절 나사를 돌린다. |
| 에어컨 스텝모터가 떨기만 함 | D8~D11과 ULN2003 IN1~IN4 순서를 확인한다. |
| 공기청정기 스텝모터가 떨기만 함 | A0~A3과 ULN2003 IN1~IN4 순서를 확인한다. |
| 가습기 분무가 약함 | 필터를 충분히 적시고 5V 전원이 안정적인지 확인한다. |
| 앱은 저장되지만 Arduino가 안 움직임 | 프론트와 백엔드가 모두 localhost로 실행 중인지 확인한다. |

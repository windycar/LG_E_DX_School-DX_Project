# MOMent 가전제어 탭 - Arduino USB 연동 가이드

## 1. 시연 구조

```text
React 가전제어 탭 (localhost)
        ↓ REST API
FastAPI 백엔드 (localhost:8000)
        ↓ pyserial / USB
Arduino UNO
        ↓
RGB LED / 가습기 모듈 / 스텝모터 / LCD / 상태 LED
```

배포된 Render 서버는 발표장 노트북의 USB 포트에 접근할 수 없다. 따라서 Arduino 시연은 프론트와 백엔드를 로컬호스트로 실행한 상태에서 진행한다.

## 2. 핀 배치

| 부품 | Arduino UNO 핀 |
|---|---|
| 시연 모드 버튼 | D2 |
| RGB LED R | D3 |
| 가습기 모듈 DIN | D4 |
| RGB LED G | D5 |
| RGB LED B | D6 |
| 제습기 상태 LED | D7 |
| ULN2003 IN1 | D8 |
| ULN2003 IN2 | D9 |
| ULN2003 IN3 | D10 |
| ULN2003 IN4 | D11 |
| 공기청정기 상태 LED | D12 |
| 연결 상태 LED | D13 |
| I2C LCD SDA | A4 |
| I2C LCD SCL | A5 |

## 3. 전원 연결

| 부품 | 연결 |
|---|---|
| Arduino UNO | 노트북 USB 연결 |
| 가습기 모듈 | VCC → 5V, GND → GND, DIN → D4 |
| ULN2003 | VCC → 5V, GND → GND |
| I2C LCD | VCC → 5V, GND → GND, SDA → A4, SCL → A5 |
| RGB LED 모듈 | VCC/GND와 D3/D5/D6 연결 |
| 일반 LED | 220Ω 저항을 직렬로 연결 |

9V 어댑터를 Arduino DC 잭에 연결하여 보드 전원을 보강할 수 있다. 단, 가습기 모듈에는 9V를 직접 연결하지 않고 Arduino의 5V 핀을 사용한다.

## 4. 설치

백엔드 Python 환경에서 다음 패키지를 설치한다.

```bash
pip install pyserial
```

Arduino IDE에서 다음 라이브러리를 설치한다.

```text
LiquidCrystal I2C
Stepper
```

`Stepper`는 Arduino 기본 라이브러리다. LCD 주소가 `0x27`이 아니라면 스케치의 `LiquidCrystal_I2C lcd(0x27, 16, 2);` 부분을 `0x3F` 등 실제 주소로 변경한다.

## 5. 실행 순서

1. Arduino UNO를 USB-A to USB-B 케이블로 노트북에 연결한다.
2. `arduino/MOMent_Appliance_Demo/MOMent_Appliance_Demo.ino`를 Arduino IDE로 연다.
3. 보드와 COM 포트를 선택한 뒤 업로드한다.
4. Arduino IDE의 시리얼 모니터를 닫는다.
5. 백엔드를 로컬에서 실행한다.

```bash
cd backend
uvicorn main:app --host 127.0.0.1 --port 8000
```

6. 프론트엔드를 로컬에서 실행한다.

```bash
cd Project
npm run dev
```

7. 앱의 가전제어 탭에서 `USB 연결` 버튼을 누른다.
8. `현재 설정 전송` 버튼을 눌러 Arduino에 가전 설정을 보낸다.

## 6. Serial 명령 형식

백엔드는 Arduino에 한 줄 문자열을 보낸다.

```text
SYNC;ML=1,50,0;AC=1,24,2;HU=0,55,2;DH=0,50,2;AP=1,2,0
```

| 코드 | 의미 |
|---|---|
| `ML` | 무드등: 전원, 밝기, 색상 코드 |
| `AC` | 에어컨: 전원, 목표 온도, 풍량 |
| `HU` | 가습기: 전원, 목표 습도, 세기 |
| `DH` | 제습기: 전원, 목표 습도, 세기 |
| `AP` | 공기청정기: 전원, 풍량, 모드 코드 |

## 7. 문제 해결

| 증상 | 확인 사항 |
|---|---|
| USB 연결 실패 | Arduino IDE의 시리얼 모니터를 닫고 다시 연결한다. |
| COM 포트를 못 찾음 | Windows 장치 관리자에서 Arduino COM 번호를 확인한다. |
| LCD가 안 보임 | I2C 주소가 `0x27`인지 확인하고 대비 조절 나사를 돌린다. |
| 스텝모터가 떨기만 함 | ULN2003 IN1~IN4 배선 순서와 스케치 핀 순서를 확인한다. |
| 가습기 분무가 약함 | 필터를 충분히 적시고 5V 전원이 안정적인지 확인한다. |
| 앱은 저장되지만 Arduino가 안 움직임 | 프론트와 백엔드가 모두 localhost로 실행 중인지 확인한다. |
